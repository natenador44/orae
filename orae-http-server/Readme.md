# NKD HTTP Server
nkrolldev's HTTP Server

# Goals
- A single dependency to pull into a new REST (maybe other types in the future) server that has everything needed to create a professional grade, production ready application.
- Opinionated. No complexity around determining which libraries to use for anything.
- A helper layer on top of the opinionated dependencies to make writing server applications easier.

